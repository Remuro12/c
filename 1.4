#include <stdio.h>
int main()
{
    int as, bs, t, a = 0, b = 0;
    scanf("%d", &as);
    for (int i = 0; i < as; ++i)
    {
        scanf("%d", &t);
        a = a ^ (1 << t);
    }
    scanf("%d", &bs);
    for (int i = 0; i < bs; ++i)
    {
        scanf("%d", &t);
        b = b ^ (1 << t);
    }
//    printf("%d", a);
    b = a & b;
    b = b / 2;
    int st = 1;
    while (b != 0)
    {
        if (b % 2 == 1)
            printf("%d\n", st);
        ++st;
        b = b / 2;
    }
} // 1.4
